<!-- Page Content -->
    <div class="container">

      <!-- Page Heading -->
      <% @jokes.each do |joke| %>
      <hr />
      <!-- Project One -->
      <div class="row">
        <div class="col-md-7">
          <a href="#">
            <%= image_tag(joke.avatar.url, class: "img-responsive") if joke.avatar? %>
          </a>
        </div>
        <div class="col-md-5">
          <p></p>
          <a class="btn btn-primary" href="#">Like</a>
          <a class="btn btn-primary" href="#">Dislike</a>
        </div>
      </div>
      
      <% end %>
      <!-- /.row -->

      <hr>
    </div>